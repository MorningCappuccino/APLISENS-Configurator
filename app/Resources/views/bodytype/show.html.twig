{% extends 'base.html.twig' %}

{% block body %}
    <h1>BodyType</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ bodyType.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ bodyType.name }}</td>
            </tr>
            <tr>
                <th>Descr</th>
                <td>{{ bodyType.descr }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('bodytype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('bodytype_edit', { 'id': bodyType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
