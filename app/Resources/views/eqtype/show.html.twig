{% extends 'base.html.twig' %}

{% block body %}
    <h1>EqType</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eqType.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ eqType.name }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('eqtype_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('eqtype_edit', { 'id': eqType.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
