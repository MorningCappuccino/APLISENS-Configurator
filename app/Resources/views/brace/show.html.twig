{% extends 'base.html.twig' %}

{% block body %}
    <h1>Brace</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ brace.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ brace.name }}</td>
            </tr>
            <tr>
                <th>Descr</th>
                <td>{{ brace.descr }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('brace_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('brace_edit', { 'id': brace.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
