{% extends 'base.html.twig' %}

{% block body %}
    <h1>ProcessConnection</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ processConnection.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ processConnection.name }}</td>
            </tr>
            <tr>
                <th>Descr</th>
                <td>{{ processConnection.descr }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('processconnection_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('processconnection_edit', { 'id': processConnection.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
