{% extends 'base.html.twig' %}

{% block body %}
    <h1>EqMode</h1>

    <table>
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ eqMode.id }}</td>
            </tr>
            <tr>
                <th>Name</th>
                <td>{{ eqMode.name }}</td>
            </tr>
            <tr>
                <th>Descr</th>
                <td>{{ eqMode.descr }}</td>
            </tr>
            <tr>
                <th>Img</th>
                <td>{{ eqMode.img }}</td>
            </tr>
        </tbody>
    </table>

    <ul>
        <li>
            <a href="{{ path('eqmode_index') }}">Back to the list</a>
        </li>
        <li>
            <a href="{{ path('eqmode_edit', { 'id': eqMode.id }) }}">Edit</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
