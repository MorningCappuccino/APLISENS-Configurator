{% extends 'base.html.twig' %}

{% use 'bootstrap_3_layout.html.twig' %}
{% form_theme edit_form _self %}


{% block integer_widget %}
    <div class="integer_widget">
        {% set type = type|default('o') %}
        {{ block('form_widget_simple') }}
    </div>
{% endblock %}


{% block body %}
    {{ dump() }}
    <h1>EquipmentMode edit</h1>

    {{ form_start(edit_form) }}

       {{ form_widget(edit_form) }}

{#<ul>
    {% for measurementRange in edit_form.measurementRanges %}
        <li>{{ form_widget(measurementRange) }}</li>
    {% endfor %}
</ul>#}
  {#{{  form_widget(edit_form.measurementRanges) }}#}


    <ul class="nav nav-pills">
        <li>
            <a class="btn btn-default" href="{{ path('eqmode_index') }}">Back to the list</a>
        </li>
        <li>
            <input class="btn btn-primary" type="submit" value="Edit" />
            {{ form_end(edit_form) }}
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input class="btn btn-danger" type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>

{% endblock %}
